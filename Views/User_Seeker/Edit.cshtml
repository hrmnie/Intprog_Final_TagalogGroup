@model AdventureSeekers.Models.User_Seeker

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}

<h1>Edit</h1>

<h4>User_Seeker</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="seeker_id" />
            <div class="form-group">
                <label asp-for="seeker_name" class="control-label"></label>
                <input asp-for="seeker_name" class="form-control" />
                <span asp-validation-for="seeker_name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="seeker_email" class="control-label"></label>
                <input asp-for="seeker_email" class="form-control" />
                <span asp-validation-for="seeker_email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="seeker_contact" class="control-label"></label>
                <input asp-for="seeker_contact" class="form-control" />
                <span asp-validation-for="seeker_contact" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="seeker_address" class="control-label"></label>
                <input asp-for="seeker_address" class="form-control" />
                <span asp-validation-for="seeker_address" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
