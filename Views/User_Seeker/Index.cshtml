@model IEnumerable<AdventureSeekers.Models.User_Seeker>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.seeker_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.seeker_email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.seeker_contact)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.seeker_address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.seeker_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.seeker_email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.seeker_contact)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.seeker_address)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.seeker_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.seeker_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.seeker_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
